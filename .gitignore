# Node & Python cache files (if ever generated)
__pycache__/
*.pyc
*.pyo
*.pyd
node_modules/
npm-debug.log*

# Sphinx build artifacts (local builds)
_build/
doctrees/
*.doctree
*.log

# Editor and OS-specific files
*.swp
*~
.DS_Store
Thumbs.db
ehthumbs.db
*.bak

# Git & GitHub metadata files (keep CNAME if you use it)
.git/
.gitignore
.github/
*.lock
*.env

# Virtual environment (in case someone accidentally adds it)
venv/
.env/
.envrc

# Temp files from scripts
*.tmp
*.temp
*.log
*.out

# Python virtual environments
pip-wheel-metadata/

# Ignore Python packages and their caches
*.egg-info/
*.egg
.Python
.pytest_cache/

# Optional: If your process auto-generates sitemap.xml, exclude it if desired
# sitemap.xml

# Exclude TTL and ontology config files if they don’t belong in the `gh-pages` branch
ontology_config.yml
*.ttl

# Ignore everything in docs/_build EXCEPT html
docs/_build/*
!docs/_build/html/
!docs/_build/html/**

# Optional: Exclude old "versions" if you only want to keep latest versions
# docs/_build/html/versions/*

# Ignore IDE settings
.vscode/
.idea/

# Optional: Exclude context files if generated in a different process
context/context.json

